<%args>
$error => undef
</%args>

<form name="upload" method="post" action="/memo/create" 
      enctype="multipart/form-data">
  <p>
    Type in a some tags:
    <br/>
    <textarea name="tags" cols="80" rows="5"></textarea>
  </p>
  <br/>
  <input type="submit" name="submit" value="search" />
</form>

% # print $c->response->body(Data::Dumper->Dump([$c]));
